// UPSTREAM-OWNED SHAPE (wasi-gfx org) — vendored ONLY so the optional
// `connection` interface validates; the 0.0.2 form is wandr's proposed
// change-set (capability-granted context), see
// proposals/wasi-surface/DESIGN.md. Never shipped from this tree.
package wasi:graphics-context@0.0.2;

interface graphics-context {
    resource context {
        get-current-buffer: func() -> abstract-buffer;
        present: func();
    }
    resource abstract-buffer;
}
